package com.mycompany.cps2002.farrugia.bonello;

/**
 * An enum representing a Filter's type (genre filter, title filter etc..). This is used to check for any overlapping filters.
 * @author James
 */
public enum FilterType {
    YOP,
    GENRE,
    TITLE,
    COMPOSITE
}
